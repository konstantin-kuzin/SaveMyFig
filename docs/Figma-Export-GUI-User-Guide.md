# Руководство пользователя: Figma Export GUI

Figma Export GUI — Electron‑приложение, которое упрощает работу с CLI резервного копирования Figma. GUI помогает подготовить `.env`, поставить зависимости и запустить `npm run run-backup` без терминала. Актуально для версии GUI 1.0 (Electron 28 / Playwright 1.48).

---

## 1. Требования
- **macOS** 11 (Big Sur) или новее, Intel/Apple Silicon.
- **Node.js** 20 LTS и npm 10+ (устанавливаются вручную через nvm/Homebrew или с официального сайта).
- **Доступ в интернет** для установки npm пакетов и Playwright браузеров.
- **Figma Personal Access Token** и действующие аккаунты, из которых разрешено скачивать файлы.
- **Права записи** в папку, куда будут выгружаться файлы (`DOWNLOAD_PATH`).

Совет: в терминале выполните `node --version`. Если команда не найдена — установите Node.js и перезапустите терминал.

---

## 2. Подготовка репозитория
### 2.0 Быстрый старт без терминала (рекомендуется для неподготовленных пользователей)
Дважды кликните `install.command` (может поставляться как `start-gui.command`) в корне репозитория. Скрипт:
- скачает portable Node.js 20.17.0, если npm не найден;
- установит зависимости в корне, Playwright Chromium и зависимости `gui/`;
- запустит GUI (production build при наличии `gui/dist`).
Логи установки: `.install.log` (корень), portable Node кладётся в `.node/` (активация в терминале: `source .node/activate`).

### 2.1 Ручная установка
```bash
# 1. Клонируйте репозиторий
git clone https://github.com/konstantin-kuzin/Figma-export.git
cd Figma-export

# 2. Установите зависимости CLI (можно также через GUI)
npm install
npx playwright install

# 3. Создайте .env на основе шаблона
mkdir -p .userData
cp .env.example .userData/.env
```
`.env` хранится в `.userData/.env`. Его можно заполнить вручную или через вкладку Config.

> Папка `.userData/` предназначена для всех файлов с чувствительными данными: туда же автоматически сохраняются `figma_backups.db`, `files.json`, Playwright отчёты и пользовательские состояния.

---

## 3. Запуск GUI
1. Откройте терминал и перейдите в `gui/`:
   ```bash
   cd gui
   npm install
   npm run start   # очистка dist, сборка и запуск electron .
   ```
   Альтернативы: из корня `npm run start --workspace gui` или `npm run start` (root скрипт проксирует в workspace).
2. Появится окно “Figma Export GUI v1.0”. Если видите пустой экран — убедитесь, что `npm run build` отработал без ошибок.

> `start-gui.command` и другие старые скрипты отсутствуют. Используйте `npm run start`.

---

## 4. Навигация по интерфейсу
Боковое меню содержит четыре вкладки:

1. **Installation** — проверка Node/npm и установка зависимостей (`npm install`, `npx playwright install` в корне).
2. **Backup** — запуск `npm run run-backup`, просмотр лога и статуса.
3. **Statistics** — чтение `figma_backups.db` (карточки + таблица файлов, ⚠ если файл не найден на диске).
4. **Config** — чтение/запись `.userData/.env` (email, auth cookie, access token, путь, списки TEAMS/PROJECTS).

> Экран Diagnostics присутствует в коде, но ссылка в меню закомментирована. Он показывает версии Node/npm и временный лог.

Переключение вкладок не сохраняет состояние автоматически. После изменения `.env` всегда нажимайте `Save settings`.

---

## 5. Настройка конфигурации
### 5.1 Что нужно подготовить
- **Figma Access Token** (`figd_...`). Получается в Developer Settings → Personal Access Tokens.
- **Auth cookie** (`__Host-figma.authn`). Откройте браузер, войдите в Figma → DevTools → Application → Cookies.
- **Team ID / Project ID**. Можно скопировать из URL Figma (`https://www.figma.com/files/team/<TEAM_ID>` или `https://www.figma.com/project/<PROJECT_ID>`).
- **Путь для скачивания**. Рекомендуется пустая папка на локальном диске, например `/Users/you/Figma-backups`.

### 5.2 Порядок действий на вкладке Config
1. Нажмите `Select` и выберите папку для загрузки.
2. Заполните email аккаунта и auth cookie (используются Playwright‑скриптами). Если cookie оставить пустым, тест попробует авторизацию по паре email+password (поля пароля в GUI нет — добавьте `FIGMA_ACCOUNT_1_PASSWORD` вручную в `.userData/.env`).
3. Укажите Personal Access Token.
4. Добавьте `PROJECTS` или `TEAMS` (любое одно поле обязательно). ID можно вводить через пробел или запятую.
5. Нажмите `Save settings`. В случае ошибки появится alert с перечнем проблем.
6. Для второго и последующих аккаунтов отредактируйте `.userData/.env` вручную, добавив переменные `FIGMA_ACCOUNT_2_EMAIL`, `FIGMA_ACCOUNT_2_AUTH_COOKIE` и т.д. Playwright будет переключаться между ними автоматически.

> GUI записывает `WAIT_TIMEOUT="10000"`. Если нужно другое значение или лимит файлов, правьте `.env` вручную (`MAX_FILES` задается в `backup/get-team-files.js` и `backup/get-project-files.js`).

---

## 6. Запуск скачивания
1. Убедитесь, что `.env` заполнен и зависимости установлены (вкладка Installation → `Install Dependencies`).
2. Перейдите на вкладку **Backup** и нажмите `Start backup`.
3. GUI запустит `npm run run-backup`. Лог появится в текстовом поле; при необходимости нажмите `Clear log`.
4. Индикатор статуса показывает текущее состояние. После успешного завершения появится `Completed`. Ошибки выводятся текстом.
5. Скачанные файлы лежат в `DOWNLOAD_PATH/<team(optional)>/<Project name (project.id)>/<File name (file.key)>.fig`.
6. Если процесс завис или нужно остановить его вручную — пока только через терминал (`ctrl + C` в соответствующем процессе). Кнопка Stop в GUI закомментирована.

### Что делает `npm run run-backup`
- Удаляет старый `files.json`.
- Генерирует новую очередь: до 3 файлов за прогон для TEAMS и до 20 для PROJECTS (`MAX_FILES` в `backup/get-*-files.js`), используя приоритет из БД.
- Запускает Playwright в headed‑режиме, авторизуется в Figma и скачивает файлы, обновляя `figma_backups.db`.
- Если смонтирован диск `/Volumes/Design Backup/Figma/HEXA UI`, можно раскомментировать блок `rsync` в `backup/run-backup.js` и подправить путь под своё окружение.

---

## 7. Просмотр статистики
1. Перейдите на вкладку **Statistics**.
2. В верхнем блоке отображаются три числа:
   - `Total files` — размер таблицы `backups`.
   - `Needing backup` — количество записей, где `last_modified_date > last_backup_date` или `last_backup_date` пуст.
   - `With errors` — сколько файлов ожидают повторной попытки (`next_attempt_date` установлена).
3. Таблица показывает проект, имя файла (кликабельно открывает Figma), даты последнего бэкапа/изменения и ключ.
4. Для сброса статуса ошибок нажмите `Reset errors`. Записи с `next_attempt_date` станут доступны очереди.
5. Поиск доступен через кнопку 🔍 (открывает поле для ввода имени файла); других фильтров пока нет. Файлы, отсутствующие на диске, подсвечены ⚠.

---

## 8. Диагностика
Экран Diagnostics есть в коде, но не отображается в боковом меню (ссылка закомментирована). Если включить его вручную:
- Показывает `Node.js версия/путь`, `npm версия/путь` и статическую `GUI version`.
- Кнопки `🔄 Обновить`/`🗑️ Очистить` работают с временным textarea; чтения реальных логов нет.
- Основные логи GUI находятся в `.userData/logs/figma-export-gui.log` (в прод-сборке — `~/Library/Application Support/Figma Export GUI/.userData/logs/figma-export-gui.log`).

---

## 9. Частые проблемы и решения
| Симптом | Возможная причина | Что делать |
| --- | --- | --- |
| `Node.js: Not installed` | Node не установлен или не в `$PATH` | Установите Node.js 20 через [nodejs.org](https://nodejs.org) или `brew install node@20`, перезапустите GUI. |
| `FIGMA_ACCESS_TOKEN must start with "figd_"` | Токен введён неверно | Создайте новый PAT в настройках Figma и вставьте полностью. |
| `PROJECTS/TEAMS are not defined...` в логе | В `.env` нет ни одного ID | Заполните `PROJECTS` или `TEAMS` (можно оба) и перезапустите `Start backup`. |
| Скачивание останавливается после нескольких файлов | Лимит очереди: 3 файла для TEAMS или 20 для PROJECTS | Увеличьте `MAX_FILES` в нужном скрипте и перезапустите сборку. |
| Playwright не может авторизоваться | Cookie просрочен или email/пароль неверные | Обновите `FIGMA_ACCOUNT_1_AUTH_COOKIE` или добавьте пароль в `.env` (`FIGMA_ACCOUNT_1_PASSWORD`). |
| GUI пишет «Ошибка установки зависимостей» | Нет сети или не хватает прав записи | Проверьте интернет, запустите `npm install` из терминала, убедитесь, что папка проекта принадлежит текущему пользователю. |
| Отсутствие скачанных файлов | `DOWNLOAD_PATH` пуст или недоступен | Выберите существующую папку с правами записи, очистите `files.json` и запустите заново. |

---

## 10. Работа из терминала (альтернатива GUI)
Если графический интерфейс недоступен, можно повторить все шаги вручную:
```bash
# Обновить очередь
node backup/get-project-files.js 12345 67890
# или
node backup/get-team-files.js 13579

# Скачать файлы
npx playwright test automations/download.spec.ts
# Полный цикл
npm run run-backup
```
Все чувствительные данные (`.env`, `figma_backups.db`, `files.json`, Playwright отчёты) расположены в `.userData/` в корне репозитория. Папка `downloads/` соответствует пути, указанному в `.env`.

---

## 11. Где искать помощь
1. Проверьте `playwright-report/index.html` — в нём отражены шаги теста.
2. Просмотрите GUI‑лог `.userData/logs/figma-export-gui.log` (для прод-сборки — в каталоге приложения в `~/Library/Application Support/Figma Export GUI/.userData/logs/`).
3. Создайте issue в GitHub, приложите `.userData/.env` (без секретов), `.userData/files.json` и логи.
4. Для срочных вопросов обратитесь к ответственному разработчику (Konstantin Kuzin).

---

Гид обновляется вместе с кодовой базой. Если интерфейс изменился, а инструкция нет — создайте задачу на актуализацию.
