# SaveMy.Fig — руководство пользователя
**Автоматическое резервное копирование всех ваших Figma-файлов с удобным графическим интерфейсом**

SaveMy.Fig — это мощный инструмент для автоматического резервного копирования файлов Figma в форматах .fig, .jam и .deck. Графический интерфейс упрощает настройку и управление процессом бэкапа без необходимости работы с терминалом.

---

## 1. Ключевые преимущества

**Массовая загрузка** — Скачивайте все файлы из команд или проектов одной кнопкой


**Безопасность** — Скрипт выполняется локально, все ваши файлы хранятся только на вашем компьютере, никакие облачные сервисы не используются

**Отслеживание изменений** — Скачиваются только обновлённые файлы, очередь загрузки формируется автоматически

---

## 2. Зачем нужен бэкап Figma-файлов?
- **Защита от потери доступа:** сохраните файлы даже при блокировке аккаунта или истечении подписки
- **Архивирование проектов:** создайте долговременное хранилище завершенных работ
- **Миграция между аккаунтами:** легко переносите файлы между Figma и Pixso, когда вас забанят из-за санкций
- **Соответствие политикам безопасности:** выполняйте требования по хранению корпоративных данных

---

## 3. Быстрый старт
1. Разрешите запуск скриптов (делается один раз):
   ```bash
   chmod +x install.command backup.command
   ```
   После этого их можно открывать двойным кликом.

2. Дважды кликните **install.command** в корне проекта. 

   Скрипт:
   - проверит наличие Node.js
   - скачает и подключит portable Node.js 20.17.0, если npm не найден;
   - установит требуемые зависимости и Playwright Chromium;
   - откроет графический интерфейс.

   Запускайте **install.command** только при обновлении зависимостей или если установка прерывалась.
3. В GUI откройте вкладку **Config** и настройте приложение.
4. Перейдите на вкладку **Backup**, нажмите **Start backup** и наслаждайтесь.
5. **install.command** запускается только для первоначальной настройки. Для запуска бекапа используйте **backup.command**

---

## 4. Если нужен терминал
Только для продвинутых пользователей:
```bash
git clone https://github.com/konstantin-kuzin/Figma-export.git SaveMy.Fig
cd SaveMy.Fig
npm install
npx playwright install
mkdir -p .userData && cp .env.example .userData/.env
cd gui && npm install && npm run start   # очистка dist + build + electron .
```
В обычном сценарии достаточно `install.command` + `backup.command`.

---

## 5. Настройка конфигурации
### Что подготовить
- Figma Access Token (`figd_...`) из Developer Settings → Personal Access Tokens.
- Auth cookie `__Host-figma.authn` из браузера (DevTools → Application → Cookies).
- Team ID или Project ID (из URL Figma).
- Папку для скачивания (например `/Users/you/Figma-backups`).

### Шаги на вкладке Config
1. Нажмите `Select` и выберите папку для загрузки.
2. Заполните `FIGMA_ACCOUNT_1_EMAIL` и `FIGMA_ACCOUNT_1_AUTH_COOKIE`.
3. Введите `FIGMA_ACCESS_TOKEN`.
4. Добавьте `PROJECTS` или `TEAMS` (ID через пробел или запятую).
5. Нажмите **Save settings**. Если в Diagnostics → Figma API Access строки `FIGMA_ACCOUNT_1_EMAIL` или `FIGMA_ACCOUNT_1_AUTH_COOKIE` красные — проверьте, что ключи в `.userData/.env` записаны ровно так и перезапустите GUI.

---

## 6. Запуск бекапа
1. Убедитесь, что настройки сохранены и зависимости установлены (вкладка Installation → `Install Dependencies` при необходимости).
2. Перейдите на вкладку **Backup** и нажмите **Start backup**.
3. Будет запущен скрипт бэкапа, лог будет отображаться на экране. По завершении статус сменится на `Completed`.
4. Файлы сохраняются в `DOWNLOAD_PATH/<team(optional)>/<Project name (project.id)>/<File name (file.key)>.fig`.
5. Если процесс завис, остановить его можно только через терминал (`Ctrl+C` в запущенном процессе). Кнопка Stop в GUI закомментирована.

Что делает `npm run run-backup`:
- очищает очередь загрузки в `.userData/files.json`;
- получает из Figma список файлов для указанных проектов/команд
- формирует новую очередь с учетом ранее скачанных файлов, ошибок и времени обновления файлов в Figma;
- запускает Playwright в headless‑режиме, авторизуется и скачивает файлы, обновляя `figma_backups.db`.

---

## 7. Просмотр статистики
1. Откройте вкладку **Statistics**.
2. Карточки показывают `Total files`, `Needing backup`, `With errors`.
3. Таблица содержит название проекта, имя файла, даты последнего бэкапа/изменения и дату новой попытки для файлов с ошибками скачивания.
4. Поиск включается кнопкой 🔍. Файлы, отсутствующие на диске, помечены ⚠.

---

## 8. Диагностика
- Показывает версии Node/npm и статичную GUI version.
- Кнопки Refresh/Clear работают с логом `.userData/logs/savemyfig.log` (`~/Library/Application Support/SaveMy.Fig/.userData/logs`).

## 9. Структура
```
SaveMy.Fig
├── /.userData               # Пользовательские данные
│   ├── .env                 # Конфигурация
│   ├── figma_backups.db     # База данных
│   └── files.json           # Очередь файлов
├── /backup                  # CLI скрипты бекапа
├── /gui                     # Графическая оболочка
└── /automations             # Playwright автоматизации
```

Требования
- macOS 11 (Big Sur) или новее.
- Node.js 20 LTS.

---

## 10. Где искать помощь
1. `playwright-report/index.html` — подробный отчёт шага скачивания.
2. Лог GUI: `.userData/logs/savemyfig.log` (`~/Library/Application Support/SaveMy.Fig/.userData/logs`).
3. GitHub issue: приложите `.userData/.env` (без секретов), `.userData/files.json` и логи.
4. Ответственный разработчик: Konstantin Kuzin.
