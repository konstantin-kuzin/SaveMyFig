# PRD: SaveMy.Fig

## Версия документа: 2.1
**Дата обновления:** 23 ноября 2025  
**Автор:** Konstantin Kuzin
**Статус:** MVP собран, требуется доработка по списку backlog

---

## 1. Обзор продукта
### 1.1 Проблема
Команде необходимо регулярно выгружать `.fig/.jam/.deck` файлы из Figma, а штатный терминальный процесс (`npm run run-backup`) сложен для людей без технической подготовки. Требуется графическая оболочка, которая:
- Заменяет ручной терминал запуском в один клик.
- Показывает, какие файлы ещё не скачаны и почему.
- Помогает заполнить `.env` без чтения документации.

### 1.2 Решение
SaveMy.Fig — Electron‑приложение поверх существующего CLI. GUI не переписывает бизнес‑логику, а вызывает готовые Node‑скрипты и Playwright тесты. Основные преимущества:
- Проверка окружения (Node.js, npm) перед установкой.
- Форма для токенов и ID проектов.
- Вкладка мониторинга скачиваний и статистики из SQLite.
- Черновая диагностическая панель (пока скрыта в меню).

### 1.3 Персоны
| Персона | Описание | Основные задачи |
| --- | --- | --- |
| **Дизайн‑менеджер** | Пользователь без доступа к терминалу | Настроить токены и запускать бэкапы.
| **DevOps / администратор** | Технический специалист | Подготовить окружение, следить за состоянием очереди и устранять ошибки.

---

## 2. Пользовательские сценарии
| ID | Сценарий | Статус реализации |
| --- | --- | --- |
| US1 | Проверить, установлен ли Node.js/npm, и поставить зависимости | ✅ Вкладка Installation запускает `npm install` + `npx playwright install`. Установка Node делается отдельным скриптом `install.command` (скачивает portable Node 20.17.0, если npm отсутствует). |
| US2 | Заполнить или обновить `.env` без текстового редактора | ✅ Форма Config поддерживает email, auth cookie, token, download path, TEAMS/PROJECTS и пишет данные в `.userData/.env`. Остальные переменные редактируются вручную. |
| US3 | Запустить полный цикл бэкапа | ✅ Download → `Start backup` выполняет `npm run run-backup`, выводит лог и прогресс. |
| US4 | Посмотреть, какие файлы ещё не скачаны | ✅ Статистика отображает сумму записей, количество файлов с `last_modified > last_backup` и таблицу `backups`. |
| US5 | Диагностировать проблемы | ⚠️ Экран Diagnostics существует, но скрыт в меню; показывает только версии Node/npm и временный лог. Нет проверки Playwright, базы, прав доступа. |
| US6 | Управлять несколькими сценариями (retry, dry-run) | 🔜 На Download доступен только `run-backup`. Остальные команды пока запускаются из терминала. |

---

## 3. Функциональная спецификация

### 3.1 Экраны GUI (факт)
1. **Installation (welcome.ts)**
   - Проверяет `node --version` и `npm --version` сразу после загрузки.
   - Кнопка `Install Dependencies` запускает `npm install` + `npx playwright install` в корне репозитория. Прогресс выводится в лог (textarea).
   - Автоустановки Node.js внутри GUI нет; для неподготовленных пользователей предусмотрен скрипт `install.command`, который качает portable Node 20.17.0 при отсутствии npm.

2. **Backup (backup.ts)**
   - Single action: `Start backup`. Статус меняется (Ready → Running → Completed/Error).
   - Лог подставляет stdout/stderr процесса. Кнопка `Clear log` очищает textarea.
   - Кнопка `Stop backup` пока закомментирована.
   - Команда по умолчанию — `npm run run-backup`. Поля выбора других скриптов нет.

3. **Statistics (statistics.ts)**
   - Карточки с общими числами (`total`, `needingBackup`, `withErrors`).
   - Таблица с колонками: Project, File Name (ссылки на Figma), Last Backup, Last Modified, File Key.
   - Есть кнопка показа поля поиска (разворачивает инпут); других фильтров нет.
   - Кнопка `Reset errors` очищает `next_attempt_date` для всех записей, разрешая повторные скачивания.

4. **Config (config.ts)**
   - Читает `.userData/.env` через IPC `read-env` и заполняет поля: `FIGMA_ACCOUNT_1_EMAIL`, `FIGMA_ACCOUNT_1_AUTH_COOKIE`, `FIGMA_ACCESS_TOKEN`, `DOWNLOAD_PATH`, `PROJECTS`, `TEAMS`.
   - Кнопка `Select` вызывает `dialog.showOpenDialog` и подставляет абсолютный путь.
   - `WAIT_TIMEOUT` жёстко прописан в коде (`10000`) и не редактируется пользователем.
   - При `Save settings` выполняется `validate-config` (проверка токена, пути, таймаута) и `write-env` с добавлением `CONFIG_VERSION="1.0"`.
> Экран Diagnostics (settings.ts) есть в коде, но ссылка в сайдбаре закомментирована; экран показывает версии Node/npm и временный лог, не читая реальные логи.

### 3.2 Взаимодействие с CLI
- GUI не меняет скрипты — все команды выполняются в корне репозитория (`process.cwd()` при запуске Electron).
- Систему можно использовать без GUI, выполняя `npm run run-backup` напрямую.
- GUI пишет `.userData/.env`, но не управляет остальными файлами внутри `.userData/` (`figma_backups.db`, `files.json`) или путями `rsync`. Эти элементы следует настраивать вручную.

### 3.3 Валидации и ошибки
- Config проверяет только наличие токена и download path, а также диапазон `WAIT_TIMEOUT`.
- Ошибки установки/запуска выводятся в обычные диалоги (`alert`). Никакой журнализации в UI нет, но Logger пишет файл.
- Если `TEAMS` и `PROJECTS` пустые, `run-backup` завершится сообщением «Skipping file generation» — пользователь должен заполнить хотя бы одно поле.

---

## 4. Нефункциональные требования
| Категория | Требование | Статус |
| --- | --- | --- |
| Платформы | macOS 11+ (Intel или Apple Silicon). | ✅ Тестировано на macOS 13/14. |
| Node.js | Версия 20 LTS. GUI только проверяет, не устанавливает автоматически. | ⚠️ Пользователь ставит сам (через nvm/brew). |
| Сеть | Обязателен интернет для установки npm и доступа к Figma API. | ✅ |
| Безопасность | `nodeIntegration: false`, но `contextIsolation` пока отключён. | ⚠️ Нужно включить перед продакшеном. |
| Логи | GUI хранит до 5 файлов по 10 МБ в `~/Library/Application Support/SaveMy.Fig/.userData/logs`. | ✅ |

---

## 5. Backlog / следующий релиз
1. **Node installer** — добавить сценарий установки Node.js (brew / bundled).
2. **Расширенная конфигурация** — множественные аккаунты, редактирование WAIT_TIMEOUT/MAX_FILES, переключатель TEAMS/PROJECTS, генерация `files.json` прямо из GUI.
3. **Download tab** — выбор команд (`start`, `retry`, `dry-run`), кнопка Stop, сохранение логов, подсветка ошибок.
4. **Statistics** — добавить поиск/фильтры/экспорт CSV и отображение `next_attempt_date`.
5. **Diagnostics** — реальные проверки (наличие Playwright браузеров, права записи в DOWNLOAD_PATH, доступность Figma API, чтение логов Logger).
6. **Hard-coded rsync** — вынести параметры в настройки или отключить по умолчанию.
7. **Packaging** — подготовить `start-gui.command` или `.app/.dmg` на основе `electron-builder` (см. `package.json:build`).
8. **UX копирайтинг** — привести интерфейс к одному языку (RU/EN) и добавить подсказки.

---

## 6. Ограничения и риски
- **API rate limits** — MAX_FILES=3 за запуск снижает нагрузку, но требует множества запусков. Нужно контролировать тайминг.
- **Playwright авторизация** — опирается на cookie (`__Host-figma.authn`). При истечении cookie тест автоматически не перезапрашивает пароль (если не заполнены `FIGMA_ACCOUNT_N_PASSWORD`).
- **Зависимость от GUI** — пользователи без Node.js не смогут работать, пока не получат помощь администратора.
- **Безопасность токенов** — `.userData/.env` хранится в открытом виде, GUI не шифрует значения.
- **Стабильность Figma UI** — Playwright скрипт кликает по конкретным ID (`mainMenu-save-as`). Любые изменения в Figma могут сломать скачивание.

---

## 7. Критерии готовности релиза (MVP)
- [x] `npm run run-backup` успешно скачивает выбранные файлы (при валидных токенах).
- [x] GUI способен установить зависимости, записать `.env` и запустить CLI.
- [x] Пользователь видит список файлов и дату последнего бэкапа.
- [ ] Пользователь может сменить тип запуска/остановить процесс без терминала.
- [ ] Нет привязки к конкретным путям разработчика (rsync, downloads).

---

## 8. Внешние зависимости
- Figma API (`https://api.figma.com/v1/projects/...`, `teams/.../projects`).
- Playwright + Chromium (устанавливаются через `npx playwright install`).
- SQLite (`sqlite3` npm пакет).
- Electron 28.

---

## 9. Открытые вопросы
1. Где будет храниться итоговая сборка GUI? Сейчас запускаем только через `npm run main`.
2. Нужно ли поддерживать Windows/Linux? Текущая автоматизация заточена под macOS (пути, `rsync`, Homebrew).
3. Кто отвечает за очистку `downloads/` и долговременное хранение архивов после `rsync`?
4. Как обрабатываем одновременный запуск нескольких копий GUI/CLI (доступ к базе и `.userData/.env` общий)?

Документ необходимо пересматривать после каждой существенной правки GUI или CLI.
