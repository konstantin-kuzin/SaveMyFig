# Figma Export GUI

Графический интерфейс для инструмента экспорта файлов Figma (Electron).

## Описание

Приложение ставит зависимости, помогает заполнить `.userData/.env` и запускает CLI бэкап (`npm run run-backup`) без терминала. Интерфейс показывает прогресс и статистику по базе `backups`.

## Требования

- macOS 11+ (Big Sur и новее)
- Node.js 20 LTS (устанавливается вручную)
- Интернет для `npm install` и Playwright

## Установка

1. Для неподготовленных пользователей: дважды кликните `install.command` (может называться `start-gui.command`). Скрипт скачает portable Node.js 20.17.0 при отсутствии npm, поставит зависимости в корне и `gui/`, установит Playwright Chromium и запустит GUI. Лог: `.install.log`, portable Node: `.node/` (активация `source .node/activate`).
2. Вручную в корне репозитория:
   ```bash
   npm install
   npx playwright install
   mkdir -p .userData
   cp .env.example .userData/.env
   ```
3. В директории `gui/`:
   ```bash
   cd gui
   npm install
   npm run start      # очистка dist + build + electron .
   ```
   Альтернатива из корня: `npm run start --workspace gui` или просто `npm run start` (root проксирует в workspace).
4. Сборка DMG: из корня `npm run build-app`.

## Использование

### Первый запуск

1. Приложение проверит версии Node.js/npm.
2. `Install Dependencies` выполнит `npm install` и `npx playwright install` в корне репозитория.
3. Установка Node.js автоматом не делается — поставьте Node 20 вручную (nvm/Homebrew/installer).

### Настройка

1. Вкладка **Config**.
2. Введите Figma Access Token (`figd_...`), email и auth cookie. Пароль при необходимости добавьте вручную в `.userData/.env` (`FIGMA_ACCOUNT_1_PASSWORD`).
3. Выберите `DOWNLOAD_PATH` и укажите `PROJECTS` или `TEAMS` (ID через пробел или запятую).
4. `WAIT_TIMEOUT` фиксирован на 10000 мс — правьте вручную в `.env`, если нужно другое значение.
5. Нажмите **Save settings**. Для второго и следующих аккаунтов добавьте `FIGMA_ACCOUNT_2_*` и т.д. вручную.

> Все чувствительные данные (`.env`, `figma_backups.db`, `files.json`, Playwright отчёты) хранятся в `.userData/` рядом с репозиторием (в прод-сборке — внутри `~/Library/Application Support/Figma Export GUI/.userData`).

### Запуск бэкапа

1. Перейдите на вкладку **Backup**.
2. Нажмите **Start backup** — запустится `npm run run-backup` (headed Playwright, очередь 3 файла для TEAMS или 20 для PROJECTS).
3. Лог и статус отображаются в реальном времени. Кнопка Stop пока закомментирована.
4. Скачанные файлы кладутся в `DOWNLOAD_PATH/<team(optional)>/<Project name (project.id)>/<File name (file.key)>.fig`.

### Просмотр статистики

- Вкладка **Statistics**: карточки `Total files`, `Needing backup`, `With errors` и таблица `backups`.
- Файлы, которых нет на диске, помечены ⚠ и попадают в очередь на повтор.
- Поиск по имени файла включается кнопкой 🔍.

### Диагностика

Экран Diagnostics есть в коде, но ссылка в меню закомментирована. Он показывает версии Node/npm и временный лог, но не читает реальные логи.

## Структура проекта

```
gui/
├── package.json              # Зависимости GUI
├── src/
│   ├── main.ts               # Main process (Electron)
│   ├── static/               # index.html и стили
│   ├── ui-*.ts               # Логика экранов (welcome, backup, statistics, config)
│   └── utils/                # preload, ScriptRunner, EnvManager, DatabaseManager, Logger
└── dist/                     # Собранные файлы (build)
```

## Разработка

```bash
# Внутри gui/
npm install

# Быстрый запуск (clean + build + electron)
npm run start

# Только сборка TypeScript
npm run build

# Дистрибутив DMG
npm run build-app   # запускать из корня, использует electron-builder
```

## Лицензия

MIT

## Автор

Konstantin Kuzin
